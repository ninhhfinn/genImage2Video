node_modules/

# ── Binary đã build ──
backend/img2video
backend/image2video
backend/fontspike
*.exe

# ── Output chẩn đoán / spike ──
backend/fontspike-out/

# ── Cài đặt & bộ nhớ local của Claude Code ──
.claude/

# ── Mockup & ảnh mẫu (fixture cho test visual — test tự skip nếu thiếu) ──
*mockup*.html
mockphotos/
mock-assets/
mockup-assets/

# ── Ảnh/video test rời ở thư mục gốc (dist/ và backend/assets vẫn được commit) ──
/*.jpg
/*.jpeg
/*.png
/*.webp
/*.mp4

# ── Output render/test sinh ra (không commit) ──
real-render-out/
realapi-out/
realapi-video-out/
shot-out/
scratch/

# ── Rác hệ điều hành ──
.DS_Store
Thumbs.db

# ── Dữ liệu module Dọn dẹp ──
# housekeeping.db chứa tài khoản, chấm công, và photos/ chứa ảnh trong nhà khách.
# Không bao giờ được commit. Sao lưu bằng cách copy cả thư mục.
hk_data/
backend/hk_data/

# ── Token Dayladau ──
# Token đọc doanh thu. Mặc định nằm ở ~/.dayladau_token (ngoài repo), nhưng chặn
# thêm ở đây phòng trường hợp ai đó để nhầm vào thư mục dự án.
.dayladau_token
*.token
